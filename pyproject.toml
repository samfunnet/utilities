[project]
name = "utilities"
version = "0.1.0"
description = "General utilities for Mikaelkirken and related projects"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "segno>=1.6.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
